---
status: draft
---

# Add refactoring to the iterate loop

Add a `refactor` action to the iterate prompt so the loop can
trigger refactoring after implementation. Depends on both the
iterate prompt and a refactor prompt existing first.
