* TODO Run slash commands from oneshot mode
Figure out how to invoke skills from =claude -p=. Possible approaches:
- Shell wrapper that reads SKILL.md and passes as prompt
- =--append-system-prompt-file= to load skill as system prompt
- Extract "dev", "plan", "continue" soft skills from spec.org
  into =/pm-dev=, =/pm-plan= skill files
* TODO Write about replacing Gerrit code review with inline jj + Claude Code review
The pm-submit / pm-feedback workflow embeds diffs and review comments
directly in jj commit messages, eliminating the need for a separate
code review tool like Gerrit.
* TODO Write about commit promotion workflow (sink toward dev)
The promote workflow sinks reviewed commits down the stack toward
dev, using conflict detection to identify dependencies. Commits
that conflict are marked [BLOCKED], revealing which reviews to
prioritize. Deterministic process via =just promote=.
* TODO Document command progression maturity levels
Commands evolve through stages as they prove useful:
1. Description in a Claude skill (SKILL.md) — Claude interprets instructions
2. Script embedded in Claude skill — reusable but coupled to skill
3. Standalone script — usable outside Claude
4. Just command — discoverable via =just -l=, composable
5. First-class command — standalone binary/script on PATH, no just dependency
6. Emacs command — streamlines multi-step terminal workflows with
   state and inspection. Example: =jj submittable= → =jj show foo=
   → =jj submit foo= becomes a single emacs command that shows the
   top of the submittable stack, lets you inspect, and submits.
Each stage adds independence from the scaffolding that preceded it.
* TODO Surface related commits for review
Commits that don't touch the same files but share a theme (e.g.
"add locking to scripts" and "add locking to skills") should be
surfaced as related so a reviewer can consider them together. Could
use commit message similarity, shared keywords, or LLM-based
semantic grouping. Useful during review and when selecting feedback
commits to work on.
* TODO emacs: Route draft-reply back to origin buffer
Update create draft command to send the data back to the buffer
where it came from (assuming origin buffer is a claude-code buffer).
Otherwise use default behavior and send to the default claude-code
buffer, or show selection in case of multiple.
* TODO Create a skill to look up man pages
Auto-trigger when unsure how to use a system tool. Read the man page
and apply what it says rather than guessing at flags.
