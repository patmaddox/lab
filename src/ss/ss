#!/bin/sh
set -eu
set -o pipefail

targets=""
deps=""

target() {
	targets="${1} ${targets}"
}

needs() {
	deps="${1} ${deps}"
}

. all.ss

for target in ${targets}; do
	echo ${target} >&2
	${target}
done
