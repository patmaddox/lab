#!/bin/sh
set -e

MSG_ID="$1"
OUTDIR="/tmp/claude-replies"
SCRIPT_DIR="$(dirname $(realpath $0))"

system_prompt=$(cat ${SCRIPT_DIR}/../lib/mail-reply.prompt)

mkdir -p "$OUTDIR"

notmuch show --format=raw "$MSG_ID" | claude -p --append-system-prompt "${system_prompt}"
